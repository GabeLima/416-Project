import React from 'react';
import Button from '@material-ui/core/Button'

// Header Bar component that appears on most (all?) pages
// Home button
// Search bar
// Comic/Story Toggle
// If not logged in: Login button?
// If logged in:
// Dropdown:
// User profile
// Account management
// Start Game button
// Join Game
// Logout
const HeaderBar = (props) => {

    return (
        <div>
            <h1>Header Bar Component</h1>
            <Button>Button!</Button>
        </div>
    );
}

export default HeaderBar;